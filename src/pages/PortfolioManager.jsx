import { useState } from 'react';import { Link } from 'react-router-dom';import PageShell from '@/components/PageShell';import PortfolioSteps from '@/components/portfolio/PortfolioSteps';import UploadWorkspace from '@/components/portfolio/UploadWorkspace';import PortfolioPrompt from '@/components/portfolio/PortfolioPrompt';import ExistingPortfolioImport from '@/components/portfolio/ExistingPortfolioImport';import PortfolioStudio from '@/components/portfolio/PortfolioStudio';import usePortfolioBuilder from '@/hooks/usePortfolioBuilder';
export default function PortfolioManager(){const builder=usePortfolioBuilder();const [saving,setSaving]=useState(false);const [notice,setNotice]=useState('');if(builder.loading)return <PageShell><div className="flex min-h-[70vh] items-center justify-center">Loading your studio…</div></PageShell>;if(!builder.profile)return <PageShell><section className="mx-auto min-h-[65vh] max-w-3xl px-5 py-24 text-center"><h1 className="text-5xl font-semibold tracking-[-.05em]">Create your creative profile first.</h1><Link to="/profile" className="mt-7 inline-block rounded-full bg-black px-6 py-3 text-white">Create Profile</Link></section></PageShell>;const persist=async publish=>{setSaving(true);try{await builder.save(publish);setNotice(publish?'Your portfolio is live.':'Draft saved.')}catch(error){setNotice(error.message||'Could not save your portfolio.')}finally{setSaving(false)}};return <PageShell><section className="mx-auto max-w-[1500px] px-5 py-14 lg:px-10"><p className="section-kicker">My Portfolio</p><h1 className="max-w-5xl text-6xl font-semibold leading-[.92] tracking-[-.065em] sm:text-8xl">Drop your work. Build your world.</h1><p className="mt-6 max-w-2xl text-xl text-black/60">Upload what you’ve made and turn it into a portfolio that feels like you.</p><div className="mt-12"><PortfolioSteps stage={builder.stage} published={builder.profile.portfolio_status==='published'}/></div>{notice&&<p className="mt-5 border-l-2 border-black pl-3 text-sm">{notice}</p>}{builder.stage==='upload'?<div className="mt-12"><UploadWorkspace assets={builder.assets} uploading={builder.uploading} error={builder.error} onFiles={builder.uploadFiles} onMove={builder.moveAsset} onSelect={builder.changeAsset} onRemove={builder.removeAsset} onHero={hero_image=>builder.setConfig({...builder.config,hero_image})} heroImage={builder.config.hero_image}/>{builder.assets.some(item=>item.selected)&&<PortfolioPrompt onGenerate={builder.generate} busy={builder.generating}/>}<ExistingPortfolioImport savedUrl={builder.profile.existing_portfolio_url} onSave={builder.importUrl}/></div>:<PortfolioStudio builder={builder} saving={saving} onSave={()=>persist(false)} onPublish={()=>persist(true)}/>}</section></PageShell>}