import PageShell from '@/components/PageShell';import CreativeCard from '@/components/CreativeCard';import { creatives } from '@/data/marketplace';
export default function Saved(){const ids=JSON.parse(localStorage.getItem('savedCreatives')||'[]');const list=creatives.filter(c=>ids.includes(c.id));return <PageShell><section className="mx-auto min-h-[60vh] max-w-[1500px] px-5 py-10 lg:px-10"><p className="section-kicker">Your shortlist</p><h1 className="text-5xl font-semibold tracking-[-.05em]">Saved creatives</h1>{list.length?<div className="mt-7 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{list.map(c=><CreativeCard key={c.id} creative={c} saved/>)}</div>:<div className="py-16 text-center"><p className="text-2xl">Your shortlist is waiting.</p><p className="mt-2 text-black/55">Save creatives from Discover to compare their work here.</p></div>}</section></PageShell>}