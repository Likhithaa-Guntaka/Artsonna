const tabs=[['inquiry','Inbox'],['pending','Pending'],['active','Active'],['completed','Completed']];
export default function ProjectsTabs({active,onChange,projects}){return <div className="flex gap-7 overflow-x-auto border-b border-black/15">{tabs.map(([value,label])=>{const count=projects.filter(p=>p.status===value).length;return <button key={value} onClick={()=>onChange(value)} className={`whitespace-nowrap border-b-2 pb-3 text-xs font-semibold uppercase tracking-[.14em] ${active===value?'border-black':'border-transparent text-black/45'}`}>{label}{count>0&&<span className="ml-2 text-black/40">{count}</span>}</button>})}</div>}