export default function AudioWaveform({levels,active=false,label='Audio waveform'}){return <div role="img" aria-label={label} className="flex h-full min-h-16 items-center gap-1 overflow-hidden" aria-live="off">{levels.map((level,index)=><span key={index} className={`audio-wave-bar block min-w-0 flex-1 rounded-full bg-current ${active?'audio-wave-active':''}`} style={{height:`${Math.max(10,level*100)}%`,animationDelay:`-${index*47}ms`,animationDuration:`${720+(index%7)*75}ms`}}/>)}</div>}