import { Play } from 'lucide-react';
import { Image } from '@/components/ui/image';
const Tile=({item,className})=><div className={`relative overflow-hidden bg-muted ${className}`}><Image src={item.image} alt={item.title} className="h-full w-full transition duration-500 group-hover:scale-[1.02]"/>{item.type==='video'&&<span className="absolute inset-0 flex items-center justify-center"><span className="flex h-11 w-11 items-center justify-center rounded-full bg-background/90"><Play className="h-4 w-4 fill-foreground"/></span></span>}</div>;
export default function CreativeWorkPreview({creative}){const work=creative.work||[];const fallback={image:creative.image,title:creative.name,type:'image'};return <div className="grid h-full grid-cols-[1.45fr_.8fr] gap-1.5"><Tile item={work[0]||fallback} className="h-full"/><div className="grid min-w-0 grid-rows-2 gap-1.5"><Tile item={work[1]||fallback} className="h-full"/><Tile item={work[2]||fallback} className="h-full"/></div></div>}