import { Image } from '@/components/ui/image';
export default function EventCard({event,joined,onJoin}){return <article><div className="aspect-[4/3] overflow-hidden bg-stone-200"><Image src={event.image} alt={event.title} className="h-full w-full" /></div><div className="pt-4"><p className="text-xs uppercase tracking-[.15em] text-black/50">{event.date} · {event.type}</p><h3 className="mt-1 text-xl font-semibold tracking-tight">{event.title}</h3><p className="mt-1 text-sm text-black/60">{event.time} · {event.location}</p><div className="mt-4 flex items-center justify-between"><span className="text-sm">{event.attending+(joined?1:0)} attending</span><button onClick={()=>onJoin?.(event.id)} className={`rounded-full px-4 py-2 text-sm ${joined?'bg-stone-200':'bg-black text-white'}`}>{joined?'Joined':'Join event'}</button></div></div></article>}