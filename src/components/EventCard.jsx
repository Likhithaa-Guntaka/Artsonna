import { Link } from 'react-router-dom';import { Bookmark } from 'lucide-react';import { Image } from '@/components/ui/image';
export default function EventCard({event,joined,saved,onJoin,onSave}){return <article><Link to={`/community/events/${event.id}`} className="block aspect-[4/3] overflow-hidden bg-stone-200"><Image src={event.image} alt={event.title} className="h-full w-full" /></Link><div className="pt-4"><p className="text-xs uppercase tracking-[.15em] text-black/50">{event.date} · {event.type}</p><Link to={`/community/events/${event.id}`}><h3 className="mt-1 text-xl font-semibold tracking-tight">{event.title}</h3></Link><p className="mt-1 text-sm text-black/60">{event.time} · {event.location}</p>{event.series&&<div className="mt-3 border-l-2 border-black/20 pl-3"><p className="text-xs font-semibold uppercase tracking-[.12em]">{event.series} · {event.frequency}</p>{event.pastAttendance&&event.usualAttendance&&<p className="mt-1 text-sm text-black/55">{event.pastAttendance} past attendees · Usually {event.usualAttendance} people attend</p>}</div>}<div className="mt-4 flex items-center justify-between gap-2"><span className="text-sm">{event.attending+(joined?1:0)} attending</span><div className="flex gap-2">{onSave&&<button onClick={onSave} className="rounded-full border border-black p-2" aria-label="Save event"><Bookmark className={`h-4 w-4 ${saved?'fill-black':''}`}/></button>}<button onClick={()=>onJoin?.(event.id)} className={`rounded-full px-4 py-2 text-sm ${joined?'bg-stone-200':'bg-black text-white'}`}>{joined?'Joined':'Join event'}</button></div></div></div></article>}