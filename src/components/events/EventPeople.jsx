import { useState } from 'react';import { Image } from '@/components/ui/image';
export default function EventPeople({attendees,count}){const [expanded,setExpanded]=useState(false);const visible=expanded?attendees:attendees.slice(0,5);return <section className="mx-auto max-w-4xl py-14"><div className="flex items-end justify-between gap-5"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-black/45">05 · People going</p><h2 className="mt-3 text-3xl font-semibold tracking-[-.04em]">{count} people are going</h2></div><button type="button" onClick={()=>setExpanded(value=>!value)} className="shrink-0 border-b border-black pb-1 text-sm font-semibold">{expanded?'Show fewer ↑':'See everyone →'}</button></div><div className="mt-8 grid grid-cols-2 gap-x-5 gap-y-7 sm:grid-cols-3 md:grid-cols-5">{visible.map(person=><div key={person.name}><Image src={person.avatar} alt={person.name} className="aspect-square w-full rounded-full"/><p className="mt-3 font-semibold">{person.name}</p><p className="text-sm text-black/50">{person.discipline}</p></div>)}</div></section>}