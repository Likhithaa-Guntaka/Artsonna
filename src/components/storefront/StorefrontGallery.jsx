import { motion,useReducedMotion } from 'framer-motion';import { Link } from 'react-router-dom';import { Image } from '@/components/ui/image';
export default function StorefrontGallery({profile}){const reduce=useReducedMotion();return <section className="mx-auto max-w-[1500px] border-t border-storefront-line px-5 py-16 lg:px-10 lg:py-24"><div className="mb-10 flex items-end justify-between"><div><p className="text-xs uppercase tracking-[.2em] text-black/45">Selected Work</p><h2 className="mt-3 font-portfolioEditorial text-5xl">{profile.field}, in {profile.name.split(' ')[0]}’s orbit.</h2></div><p className="hidden text-xs uppercase tracking-[.16em] text-black/40 sm:block">New York · After Dark</p></div><div className="columns-1 gap-4 sm:columns-2 lg:columns-3">{profile.gallery.map((item,index)=><motion.figure key={`${item.src}-${index}`} initial={reduce?false:{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.15}} transition={{duration:.55,delay:reduce?0:(index%3)*.08}} className="group relative mb-4 break-inside-avoid overflow-hidden"><div className={`${item.ratio} overflow-hidden`}><Image src={item.src} alt={`${profile.name} ${profile.field.toLowerCase()} work ${index+1}`} className="h-full w-full transition-transform duration-700 ease-out group-hover:scale-[1.04]"/></div>{index===2&&<Link to={`/book/${profile.id}`} className="absolute bottom-0 right-0 bg-primary px-5 py-3 text-xs font-semibold uppercase tracking-[.12em] text-primary-foreground">Work With {profile.name.split(' ')[0]} ↗</Link>}</motion.figure>)}</div></section>}