import { Heart } from 'lucide-react';
import { Link } from 'react-router-dom';
import CreativeWorkPreview from '@/components/marketplace/CreativeWorkPreview';
export default function CreativeCard({creative,saved,onSave}){const destination=creative.portfolioPath||`/creative/${creative.id}`;return <article className="group min-w-0"><div className="relative overflow-hidden bg-muted" style={{aspectRatio:creative.aspect||'4 / 5'}}><Link to={destination} className="block h-full"><CreativeWorkPreview creative={creative}/></Link><button onClick={()=>onSave?.(creative.id)} className="absolute right-3 top-3 rounded-full bg-background/90 p-2.5" aria-label={`Save ${creative.name}`}><Heart className={`h-4 w-4 ${saved?'fill-foreground':''}`}/></button>{creative.availability&&<span className="absolute bottom-3 left-3 bg-background/90 px-2.5 py-1 text-xs font-medium">{creative.availability}</span>}</div><div className="pt-3"><div className="flex items-baseline justify-between gap-3"><Link to={destination} className="font-semibold">{creative.name}</Link>{creative.demo&&<span className="text-[9px] uppercase tracking-[.14em] text-black/35">Demo portfolio</span>}</div><p className="mt-0.5 text-sm text-black/60">{creative.role} · {creative.neighborhood}</p>{creative.tags?.length>0&&<p className="mt-2 text-[11px] uppercase tracking-[.12em] text-black/45">{creative.tags.slice(0,3).join(' · ')}</p>}</div></article>}