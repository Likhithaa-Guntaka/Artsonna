import { Heart } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Image } from '@/components/ui/image';
export default function CreativeCard({creative,saved,onSave}){return <article className="group min-w-0"><div className="relative aspect-[4/5] overflow-hidden bg-stone-200"><Link to={`/creative/${creative.id}`}><Image src={creative.image} alt={`${creative.name} portfolio`} className="h-full w-full transition duration-500 group-hover:scale-[1.02]" /></Link><button onClick={()=>onSave?.(creative.id)} className="absolute right-3 top-3 rounded-full bg-white/90 p-2.5" aria-label="Save creative"><Heart className={`h-4 w-4 ${saved?'fill-black':''}`}/></button><span className="absolute bottom-3 left-3 bg-white px-2.5 py-1 text-xs font-medium">{creative.availability}</span></div><div className="pt-3"><div className="flex justify-between gap-3"><div><Link to={`/creative/${creative.id}`} className="font-semibold">{creative.name}</Link><p className="text-sm text-black/60">{creative.role} · {creative.neighborhood}</p></div><p className="whitespace-nowrap text-sm">From ${creative.price}</p></div><p className="mt-2 text-xs uppercase tracking-[.12em] text-black/50">{creative.tags.join(' · ')}</p></div></article>}