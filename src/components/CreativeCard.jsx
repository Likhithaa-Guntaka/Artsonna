import { Link } from 'react-router-dom';
import { Image } from '@/components/ui/image';
export default function CreativeCard({creative}){const destination=`/creative/${creative.slug||creative.id}`;return <article className="group min-w-0"><Link to={destination} className="block aspect-[4/5] cursor-pointer overflow-hidden bg-muted"><Image src={creative.cover_image} alt={`${creative.name} portfolio cover`} className="h-full w-full object-cover transition-transform duration-300 group-hover:scale-[1.02]"/></Link><div className="pt-3"><Link to={destination} className="text-base font-semibold tracking-tight hover:underline hover:underline-offset-4">{creative.name}</Link><p className="mt-1 text-sm text-black/60">{creative.role} · {creative.neighborhood}</p>{creative.tags?.length>0&&<p className="mt-1.5 text-[11px] uppercase tracking-[.14em] text-black/45">{creative.tags.slice(0,3).join(' · ')}</p>}</div></article>}