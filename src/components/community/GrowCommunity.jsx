import { useState } from 'react';import WeeklyPrompt from '@/components/community/WeeklyPrompt';import CircleExplorer from '@/components/community/CircleExplorer';import OfficeHours from '@/components/community/OfficeHours';
export default function GrowCommunity(){const [active,setActive]=useState('circles');return <section className="mx-auto max-w-[1500px] px-5 pb-20 lg:px-10"><WeeklyPrompt/><div className="mt-14 flex gap-8 border-b border-black/15">{['circles','office hours'].map(tab=><button key={tab} onClick={()=>setActive(tab)} className={`border-b-2 pb-3 text-sm capitalize ${active===tab?'border-black font-semibold':'border-transparent text-black/50'}`}>{tab}</button>)}</div><div className="mt-8">{active==='circles'?<CircleExplorer/>:<OfficeHours/>}</div></section>}