import { useState } from 'react';
import { Image } from '@/components/ui/image';
import { spotlights } from '@/data/community';
export default function CreatorSpotlight(){const [index,setIndex]=useState(0);const creator=spotlights[index];return <section className="grid overflow-hidden border border-black/15 bg-background lg:grid-cols-2"><Image src={creator.portrait} alt={creator.name} className="min-h-[420px] w-full"/><div className="flex flex-col justify-between p-7 sm:p-10"><div><p className="section-kicker">Creator Spotlight</p><h2 className="text-4xl font-semibold tracking-[-.04em]">{creator.name}</h2><p className="mt-2 text-black/55">{creator.role} · {creator.neighborhood}</p><div className="mt-10 space-y-8"><div><p className="text-xs font-semibold uppercase tracking-[.16em]">What are you working on?</p><p className="mt-3 text-lg leading-relaxed text-black/70">{creator.workingOn}</p></div><div><p className="text-xs font-semibold uppercase tracking-[.16em]">Who do you want to collaborate with next?</p><p className="mt-3 text-lg leading-relaxed text-black/70">{creator.nextCollab}</p></div></div></div><button onClick={()=>setIndex((index+1)%spotlights.length)} className="mt-10 w-fit border-b border-black pb-1 text-sm font-semibold">Next story</button></div></section>}