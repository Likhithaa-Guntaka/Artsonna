import { motion,useReducedMotion } from 'framer-motion';
import { Link } from 'react-router-dom';
import { Image } from '@/components/ui/image';
import { creatives } from '@/data/marketplace';

const matches=[{creative:creatives[0],meta:'Editorial · Nightlife'},{creative:creatives[1],meta:'Cinematic · Music'},{creative:creatives[2],meta:'Fashion · Minimal'}];
export default function StoryDiscover(){const reduce=useReducedMotion();return <motion.div initial={reduce?false:{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.2}} transition={{duration:.6}} className="grid min-h-[48vh] items-center gap-12 py-14 lg:grid-cols-[.8fr_1.2fr]"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-terracotta">02 — Discover</p><h3 className="mt-4 text-4xl font-semibold tracking-[-.045em]">Marketplace match</h3><div className="mt-8 border-l-2 border-terracotta bg-background/60 p-6 text-lg leading-relaxed shadow-sm">“I need a photographer for an indie album shoot in Chinatown. Nighttime, cinematic, slightly gritty.”</div><p className="mt-6 max-w-sm text-black/60">People search for the work they want — not just a job title.</p><Link to="/marketplace" className="group mt-6 inline-flex items-center gap-2 border-b border-black pb-1 text-xs font-semibold uppercase tracking-[.14em]">Explore Marketplace <span className="transition-transform group-hover:translate-x-1">→</span></Link></div><div className="divide-y divide-black/10 border-y border-black/15">{matches.map(({creative,meta},index)=><motion.div key={creative.id} whileHover={reduce?{}:{x:5}} className="grid grid-cols-[92px_1fr_auto] items-center gap-5 py-4"><Image src={creative.image} alt={`${creative.name} work`} className="h-24 w-[92px]"/><div><p className="text-xl font-semibold">{creative.name}</p><p className="mt-1 text-sm text-black/50">{meta}</p></div><span className={`h-2.5 w-2.5 rounded-full ${index===0?'bg-terracotta':'bg-black/15'}`}/></motion.div>)}</div></motion.div>}