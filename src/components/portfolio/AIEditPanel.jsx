import { useState } from 'react';import { Loader2,Sparkles } from 'lucide-react';
export default function AIEditPanel({onApply,busy,error}){const [instruction,setInstruction]=useState('');return <section className="border border-black/15 bg-background p-5"><div className="flex items-center gap-2"><Sparkles className="h-4 w-4"/><h3 className="font-semibold">AI Edit</h3></div><p className="mt-2 text-sm text-black/50">Describe the feeling. Your work and content stay untouched.</p><textarea value={instruction} onChange={e=>setInstruction(e.target.value)} className="mt-4 min-h-24 w-full border-b border-black bg-transparent py-2 outline-none" placeholder="Make it darker, more experimental, and more like an underground fashion magazine."/><button disabled={!instruction.trim()||busy} onClick={()=>onApply(instruction)} className="mt-4 flex items-center gap-2 rounded-full bg-black px-5 py-2.5 text-sm text-white disabled:opacity-40">{busy&&<Loader2 className="h-4 w-4 animate-spin"/>}Update Portfolio</button>{error&&<p className="mt-3 text-sm text-destructive">{error}</p>}</section>}