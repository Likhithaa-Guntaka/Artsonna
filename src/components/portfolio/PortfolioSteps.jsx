const steps=['Upload Work','Describe Your Vibe','Portfolio Studio','Publish'];
export default function PortfolioSteps({stage,published}){const current=published?3:stage==='studio'?2:0;return <div className="overflow-x-auto"><div className="flex min-w-max border-b border-black/15">{steps.map((step,index)=><div key={step} className={`min-w-36 border-b-2 px-1 py-4 text-xs uppercase tracking-[.14em] ${index<=current?'border-black text-black':'border-transparent text-black/35'}`}>{String(index+1).padStart(2,'0')} · {step}</div>)}</div></div>}