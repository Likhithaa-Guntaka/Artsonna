const steps=['Work','Identity','Presence','Review','Generate'];
export default function PortfolioSteps({current}){return <div className="overflow-x-auto"><div className="flex min-w-max border-b border-black/15">{steps.map((step,index)=>{const number=index+1;return <div key={step} className={`min-w-36 border-b-2 px-1 py-4 text-xs uppercase tracking-[.15em] ${number<=current?'border-black text-black':'border-transparent text-black/30'}`}>{String(number).padStart(2,'0')} · {step}</div>})}</div></div>}