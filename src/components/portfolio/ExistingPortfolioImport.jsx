import { useState } from 'react';
export default function ExistingPortfolioImport({savedUrl,onSave}){const [open,setOpen]=useState(false);const [url,setUrl]=useState(savedUrl||'');return <section className="mt-16 border-t border-black/15 pt-8"><button onClick={()=>setOpen(!open)} className="text-sm font-semibold underline underline-offset-4">Already have a portfolio? Import Existing Portfolio</button>{open&&<form onSubmit={e=>{e.preventDefault();onSave(url)}} className="mt-5 flex max-w-2xl gap-3"><input required type="url" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://yourportfolio.com" className="w-full border-b border-black bg-transparent px-1 py-3 outline-none"/><button className="rounded-full border border-black px-5 py-2 text-sm">Save URL</button></form>}</section>}