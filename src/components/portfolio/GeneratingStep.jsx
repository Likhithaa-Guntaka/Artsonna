import { useEffect,useState } from 'react';import { motion,AnimatePresence } from 'framer-motion';
const phrases=['Understanding your work…','Finding your visual language…','Building your layout…','Writing your introduction…','Creating your portfolio…'];
export default function GeneratingStep({error,onBack}){const [index,setIndex]=useState(0);useEffect(()=>{const timer=setInterval(()=>setIndex(current=>Math.min(current+1,phrases.length-1)),1400);return()=>clearInterval(timer)},[]);return <section className="flex min-h-[65vh] flex-col items-center justify-center text-center"><p className="section-kicker">05 · Generate</p><h1 className="max-w-4xl text-6xl font-semibold tracking-[-.06em]">Building your corner of the internet.</h1><div className="mt-14 h-12"><AnimatePresence mode="wait"><motion.p key={index} initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-8}} className="text-xl text-black/55">{phrases[index]}</motion.p></AnimatePresence></div><div className="mt-8 flex gap-2">{phrases.map((_,i)=><span key={i} className={`h-1 w-12 ${i<=index?'bg-black':'bg-black/15'}`}/>)}</div>{error&&<p className="mt-8 text-destructive">{error}</p>}<button onClick={onBack} className="mt-10 rounded-full border border-black px-6 py-3">Back to Review</button></section>}