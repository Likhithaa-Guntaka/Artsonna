import AdaptivePortfolioGallery from '@/components/portfolio/AdaptivePortfolioGallery';
export default function CreativePortfolioGrid({creative}){const assets=creative.work.map((item,index)=>({id:`${creative.id}-${index}`,file_url:item.image,file_name:item.title,media_type:item.type==='video'?'video':'image',video:item.video,title:item.title,category:item.category,aspect:item.aspect,sort_order:index}));return <AdaptivePortfolioGallery assets={assets} showCaptions/>}