import { Play } from 'lucide-react';
import { Image } from '@/components/ui/image';
export default function CreativePortfolioGrid({creative}){return <div className="columns-1 gap-4 sm:columns-2 lg:columns-3">{creative.work.map((item,index)=>{const media=<div className="relative overflow-hidden bg-muted"><Image src={item.image} alt={`${item.title} — demo portfolio work`} className="w-full" style={{aspectRatio:item.aspect}}/>{item.type==='video'&&<span className="absolute inset-0 flex items-center justify-center bg-black/10"><span className="flex h-14 w-14 items-center justify-center rounded-full bg-background/90"><Play className="h-5 w-5 fill-foreground"/></span></span>}</div>;return <article key={`${item.title}-${index}`} className="mb-8 break-inside-avoid">{item.video?<a href={item.video} target="_blank" rel="noreferrer" aria-label={`Play ${item.title} on Pexels`}>{media}</a>:media}<p className="mt-3 text-xs uppercase tracking-[.14em] text-black/45">{item.category}</p><h3 className="mt-1 text-lg font-semibold">{item.title}</h3></article>})}</div>}