import { useState } from 'react';import { ArrowRight } from 'lucide-react';
const styles=['Minimal','Editorial','Playful','Bold','Cinematic','Experimental','Clean','Raw','Colorful','Monochrome'];
export default function PortfolioPrompt({onGenerate,busy}){const [prompt,setPrompt]=useState('');const [selected,setSelected]=useState([]);const toggle=style=>setSelected(current=>current.includes(style)?current.filter(item=>item!==style):[...current,style]);return <section className="mt-16 border-t border-black/15 pt-14"><p className="section-kicker">Step 02</p><h2 className="text-4xl font-semibold tracking-[-.05em]">How should your portfolio feel?</h2><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} className="mt-7 min-h-36 w-full border-b border-black bg-transparent py-4 text-2xl leading-relaxed outline-none" placeholder="Raw editorial photography, minimal typography, lots of negative space, downtown NYC energy…"/><div className="mt-6 flex flex-wrap gap-2">{styles.map(style=><button key={style} onClick={()=>toggle(style)} className={`rounded-full border px-4 py-2 text-sm ${selected.includes(style)?'border-black bg-black text-white':'border-black/25'}`}>{style}</button>)}</div><button onClick={()=>onGenerate(prompt,selected)} disabled={busy} className="mt-9 flex items-center gap-2 rounded-full bg-black px-7 py-3.5 text-white disabled:opacity-40">Generate My Portfolio <ArrowRight className="h-4 w-4"/></button></section>}