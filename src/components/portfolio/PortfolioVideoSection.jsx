import { Image } from '@/components/ui/image';
export default function PortfolioVideoSection({videos=[],pad,border}){const independent=videos.filter(video=>!video.matched_project_title);if(!independent.length)return null;return <section id="videos" className={`${pad} ${border}`}><p className="text-xs uppercase tracking-[.22em] opacity-55">Featured Videos</p><div className="mt-8 grid gap-10 md:grid-cols-2">{independent.map((video,index)=><article key={`${video.video_url}-${index}`}>{video.thumbnail_url&&<a href={video.video_url} target="_blank" rel="noreferrer" className="block aspect-video overflow-hidden"><Image src={video.thumbnail_url} alt={video.title} className="h-full w-full"/></a>}<h3 className="mt-4 text-3xl">{video.title}</h3>{video.description&&<p className="mt-2 opacity-60">{video.description}</p>}<div className="mt-4 flex flex-wrap gap-2">{[...(video.technologies||[]),...(video.skills||[])].slice(0,6).map(item=><span key={item} className="rounded-full border border-current/25 px-3 py-1 text-xs">{item}</span>)}</div><a href={video.video_url} target="_blank" rel="noreferrer" className="mt-5 inline-block border-b border-current text-sm">Watch video →</a></article>)}</div></section>}