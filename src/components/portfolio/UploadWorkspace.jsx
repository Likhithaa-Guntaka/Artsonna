import { useRef,useState } from 'react';import { ImagePlus,Loader2,Plus } from 'lucide-react';import AssetCard from '@/components/portfolio/AssetCard';
export default function UploadWorkspace({assets,uploading,error,onFiles,onMove,onSelect,onRemove,onHero,heroImage}){const input=useRef(null);const [drag,setDrag]=useState(false);const drop=e=>{e.preventDefault();setDrag(false);onFiles(e.dataTransfer.files)};return <section><div onDragOver={e=>{e.preventDefault();setDrag(true)}} onDragLeave={()=>setDrag(false)} onDrop={drop} className={`flex min-h-72 flex-col items-center justify-center border border-dashed px-6 text-center ${drag?'border-black bg-muted':'border-black/30'}`}><ImagePlus className="h-9 w-9"/><h2 className="mt-5 text-3xl font-semibold tracking-[-.04em]">Drop your work here.</h2><p className="mt-2 text-black/55">Images and videos · Select multiple files</p><button onClick={()=>input.current?.click()} disabled={uploading} className="mt-6 flex items-center gap-2 rounded-full bg-black px-6 py-3 text-white disabled:opacity-50">{uploading?<Loader2 className="h-4 w-4 animate-spin"/>:<Plus className="h-4 w-4"/>}{uploading?'Uploading…':assets.length?'Add more':'Choose files'}</button><input ref={input} type="file" accept="image/*,video/*" multiple className="hidden" onChange={e=>onFiles(e.target.files)}/></div>{error&&<p className="mt-3 text-sm text-destructive">{error}</p>}{assets.length>0&&<div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">{assets.map((asset,index)=><AssetCard key={asset.id} asset={asset} index={index} count={assets.length} onMove={direction=>onMove(index,direction)} onSelect={()=>onSelect(asset.id,{selected:!asset.selected})} onRemove={()=>onRemove(asset.id)} onHero={()=>onHero(asset.file_url)} isHero={heroImage===asset.file_url}/>)}</div>}</section>}