export default function PortfolioCredentialsSection({experience=[],education=[],certifications=[],pad,border}){if(!experience.length&&!education.length&&!certifications.length)return null;return <section id="experience" className={`${pad} ${border}`}><p className="text-xs uppercase tracking-[.22em] opacity-55">Experience & Credentials</p>{experience.length>0&&<div className="mt-8 divide-y divide-current/15">{experience.map((item,index)=><article key={`${item.organization}-${index}`} className="grid gap-3 py-6 md:grid-cols-[.7fr_1fr]"><div><h3 className="text-2xl">{item.role}</h3><p className="opacity-55">{item.organization} · {item.period}</p></div><p className="opacity-70">{item.summary}</p></article>)}</div>}<div className="mt-10 grid gap-10 md:grid-cols-2">{education.length>0&&<div><h3 className="text-2xl">Education</h3>{education.map((item,index)=><p key={`${item.institution}-${index}`} className="mt-4">{item.program}<span className="block opacity-55">{item.institution} · {item.period}</span></p>)}</div>}{certifications.length>0&&<div><h3 className="text-2xl">Certifications</h3>{certifications.map((item,index)=><p key={`${item.name}-${index}`} className="mt-4">{item.name}<span className="block opacity-55">{item.issuer} · {item.year}</span></p>)}</div>}</div></section>}