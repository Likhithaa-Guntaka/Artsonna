export default function CommunityTabs({active,onChange}){return <div className="flex gap-8 border-b border-black/15">{['events','boards'].map(tab=><button key={tab} onClick={()=>onChange(tab)} className={`border-b-2 pb-3 text-sm capitalize ${active===tab?'border-black font-semibold':'border-transparent text-black/50'}`}>{tab}</button>)}</div>}